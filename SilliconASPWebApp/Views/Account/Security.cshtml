@model SecurityViewModel
@{
    ViewData["Title"] = "Security";
}

<div class="security">
    <div class="container">

            @await Html.PartialAsync("~/Views/Shared/Partials/AccountPartials/_ProfileMenuPartial.cshtml", Model.ProfileMenuModel)
    
        <div>
            <div class="security-form">
                <h2>Security</h2>
                <div class="error-msg">@Model.ChangePasswordErrorMessage</div>

                @await Html.PartialAsync("~/Views/Shared/Partials/AccountPartials/_ChangePasswordFormPartial.cshtml", Model.Form)


            </div>
            <div class="delete-account">
                <h5>Delete account</h5>
                <p>
                    When you delete your account, your public profile will be deactivated immediately. If you
                    change
                    your mind before the 14 days are up, sign in with your email and password, and we’ll send
                    you a
                    link to reactivate your account.
                </p>

                <div class="error-msg">@Model.DeleteAccountErrorMessage</div>

                @await Html.PartialAsync("~/Views/Shared/Partials/AccountPartials/_DeleteAccountFormPartial.cshtml", Model.DeleteForm)

            </div>
        </div>
    </div>
</div>

@await Html.PartialAsync("_ValidationScriptPartial")